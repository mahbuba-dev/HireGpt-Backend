services:
  backend:
    container_name: consultedge-backend
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "${PORT:-5000}:5000"
    restart: unless-stopped
    volumes:
      - ./uploads:/app/uploads
